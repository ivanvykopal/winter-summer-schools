export interface School {
  name: string;
  link: string;
  venue: string;
  start_date: string;
  end_date: string;
  registration_status: string;
  application_deadline?: string;
  description?: string;
}
